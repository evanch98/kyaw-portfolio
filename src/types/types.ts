/* projectObject must be used whenever the new project box is created */
export type projectObject = {
  title: string;
  sourceSrc: string;
  liveSrc: string;
  date: string;
  category: string;
  tech: string[];
  desc: string[];
};
